# Add project specific ProGuard rules here.
# Minification is disabled for the debug/release testing builds (isMinifyEnabled = false),
# so no custom rules are required yet. When Firebase is enabled and minification is turned
# on, add keep rules for any reflectively-accessed model classes here.
